name: CI Docker scout

on:
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Build minitwit-image
        uses: docker/build-push-action@v2
        with:
          context: .
          file: ./Dockerfile
          push: false
          tags: ${{ secrets.DOCKER_USERNAME }}/minitwit-image:latest
          cache-from: type=registry,ref=${{ secrets.DOCKER_USERNAME }}/minitwit-image:webbuildcache
          cache-to: type=registry,ref=${{ secrets.DOCKER_USERNAME }}/minitwit-image:webbuildcache,mode=max

      - name: Docker Scout
        id: docker-scout
        run: docker scout cves --only-package express
        # uses: docker/scout-action@v1
        # with:
        #   command: compare
        #   image: ${{ secrets.DOCKER_USERNAME }}/minitwit-image:latest
        #   to-env: production
        #   organization: ${{ secrets.DOCKER_USERNAME }}
